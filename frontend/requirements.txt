fastapi>=0.115
uvicorn[standard]>=0.30
a2a-sdk~=0.3.22
httpx>=0.27
google-auth>=2.30
# Signs the per-browser identity cookie so visitors no longer share one
# conversation context and memory bank.
itsdangerous>=2.2
